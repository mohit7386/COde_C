//Here this is the simple code for explicit typecasting 
#include<stdio.h>
int main(){
    float a = 4.5,b=6.2;
    printf("Multiply of a and b is: %d",(int)(a*b)); //Here we are converting the floating point value to the integer value then it will give the result before the point
    return 0;
}